{
  "name": "custom-overlays",
  "private": true,
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:server": "pnpm -C server dev",
    "build": "pnpm -r build",
    "start:server": "pnpm -C server start",
    "mockEvent": "pnpm --filter @custom/server exec tsx src/scripts/mock-kick.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
